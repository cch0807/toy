[tool.poetry]
name = "toy"
version = "0.1.0"
description = "cch's toy project."
authors = ["cch0807 <ckdgus11021@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
